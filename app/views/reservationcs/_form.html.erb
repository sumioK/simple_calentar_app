<%= form_with(model: reservationc) do |f| %>
  <% if reservationc.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reservationc.errors.count, "error") %> prohibited this reservationc from being saved:</h2>

      <ul>
        <% reservationc.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :氏名, style: "display: block" %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :開始日時, style: "display: block" %>
    <%= f.datetime_field :start_time %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>
